# Changelog

## 0.1.0

- Backend AWS (Serverless) con REST + WebSocket, Auth con Firebase (modo transición).
- Frontend: modo híbrido Firestore/AWS (`VITE_AWS_API_BASE_URL` y `VITE_AWS_WS_URL`).
- UX: perfil y ligas con iconos (sin emojis) y estilo oscuro tipo Discord.
- Liga: navegación más ordenada (tabs en móvil, sidebar en desktop) + fixes de build.
