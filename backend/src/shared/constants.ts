export const VISIBILITIES = ["private", "public"] as const;
export const JOIN_REQ_STATUS = ["pending", "approved", "rejected"] as const;
export const POINT_REQ_STATUS = ["pending", "approved", "rejected"] as const;
export const ROLES = ["owner", "admin", "member"] as const;
